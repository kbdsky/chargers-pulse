services:
  - type: web
    name: chargers-pulse-mobile
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn newspulse.mobile.server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
